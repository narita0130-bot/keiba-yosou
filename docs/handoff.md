# 競馬EV検討プロジェクト 引き継ぎ資料

**初版: 2026年8月22日（土）** ／ **2026年8月23日（日）にClaude Codeへ移管**（1章・12章を更新）

> このファイルは `narita0130-bot/keiba-yosou` リポジトリで管理する生きた資料。
> チャットへのアップロードによる引き継ぎは不要になった（12章参照）。

---

## 0. 最優先で読む要点

1. **エンジンは復元→回帰テスト21項目全通過を確認してから使う**（後述の手順）
2. **非馬の記事の馬番は公式とズレることが頻発**（確認済み5件、8/15・8/16は今回ズレなし）。必ずJRA-VAN/netkeibaで照合
3. **パラメータ方針は「市場ベースライン＋印加点のみ」**。情報のない馬に中立値を与えてはいけない
4. **単勝は`_p_combo`未対応**。`eng.p[eng.num2i[馬番]]`から外部計算する（エンジン本体は無改変）
5. **非馬の「見解追記中」の印は完成版で変わることがある**（8/22中京7Rで◎と印が入れ替わった実例）。追記中段階を鵜呑みにせず、必ず完成版を確認してから買い目を組む
6. **前チャット「競馬シミュレーション2」がチャット上限に到達し、二度と戻れなくなった**。新しい会話は過去の生コンテキストを自動で持ち越せないため、**この3点セット（keiba_engine_v2.py／test_engine.py／このhandoff.md）を節目ごとに更新し、次の新しいチャットに必ずアップロードする運用が唯一の現実解**（詳細は8章）

---

## 1. エンジンの状態と復元手順

### ファイル（リポジトリ内。アップロード不要）
| ファイル | 内容 |
|---|---|
| `engine/keiba_engine_v2.py` | メインEV計算エンジン |
| `engine/test_engine.py` | 回帰テスト21項目 |
| `engine/run_ev.py` | 印→EV計算の実行スクリプト（8/23新設） |
| `scripts/fetch_odds.py` | netkeibaオッズ取得（単複・ワイド・馬連・馬単・3連複・3連単） |
| `scripts/build_sheet.py` | 印・買い目つき日次シート |
| `scripts/build_weekend.py` | 開催日全レースのまとめ（`--combos N`で馬連・3連複併記） |
| `scripts/build_overseas.py` | JRA発売の海外レース用 |

### セッション開始時のコマンド
```bash
cd /workspace/keiba-yosou/engine && rm -rf __pycache__ && python3 test_engine.py
```
→ 「✅ 全21項目合格」が出るまで買い目判断をしないこと。
（numpyが無い環境では `pip install -q numpy` を先に実行）

### エンジンの仕様
- Plackett-Luce厳密計算（乱数なし・毎回同一結果）
- Henery割引（λ2=0.81, λ3=0.65）
- 市場75% ＋ モデル25%ブレンド（KL最小化でγ自動較正）
- 券種別払戻率：単複0.80／ワイド・馬連0.775／馬単・3連複0.75／3連単0.725
- 頑健性判定：9摂動（λ3通り×割引3通り）の**最小EV**で判断
- 入力検証あり（オッズ1.0以下・適性範囲外・馬番重複を例外で停止）

### ⚠️ 単勝は`_p_combo`未対応（8/16発見）
`_p_combo`メソッドは複勝・ワイド・馬連・馬単・3連複・3連単のみ対応。単勝を含む買い目は下記のように外部で計算する（エンジン本体は改変しない）：
```python
idx = eng.num2i[馬番]
p_hit = eng.p[idx]          # 単勝的中率
ev = p_hit * 実配当
# 頑健区間も同様に9パターンでeng.pを再計算して求める
```

### ⚠️ 検索から断片再構築する場合の注意（8/15発見）
過去チャットの検索結果からエンジンを再構築した際、`_model_prob`内のインデックスが`h[3+k]`（誤り。正しくは`h[4+k]`）になっているバグが実際に発生した。5項目（距離適性〜状態）が全部中立値3固定の場合はγの自動較正が歪みを吸収してしまい表面化しにくいが、実際の値を入れる場面では重大な誤差になりうる。**検索での再構築は最終手段とし、可能な限り本物のファイルをアップロードしてもらうこと。**

### 判定記号
| 記号 | 条件 | 意味 |
|---|---|---|
| 🟢 | 頑健下限 ≥ 1.0 | パラメータを揺らしても期待値プラス＝買い |
| 🟡 | 中心EV ≥ 1.0 だが頑健下限 < 1.0 | 見かけの妙味の可能性 |
| ❌ | 中心EV < 1.0 | 見送り |

### 買いライン運用
`買いライン = 1 / 的中確率`（頑健版は1/最小的中率）。
**実配当がこのライン以上なら買い、未達なら見送り**。朝オッズは判断に使わず、締切直前の実配当で最終照合する。

---

## 2. パラメータ設定の方針（重要な学習）

### 現行の正しい方式
```
基礎能力 = 市場人気順に沿ったベースライン（50〜80のlog正規化）＋ 印に応じた加点
加点：◎+8 / ○+5 / ▲+3 / △+2 / ☆+2 / ★+2 / 穴+2
他5項目（距離・コース・騎手・近走・状態）は全馬3（中立）で固定
```
これにより**無印馬の乖離がほぼゼロ**になり、「予想家に乗る」意思だけが数値に反映される。

### なぜこうしたか（失敗事例）
川崎11Rで、記者コメントが一切ない②エルロイ・④クオレスに中立値を与えたところ、市場が33.8倍・55.2倍と厳しく評価していたため**相対的にモデルが+19〜20%高く見え、根拠のない妙味が発生**した。「情報がフェアじゃない時点で計算を回すべきじゃない」という指摘を受けて方針を修正。

**原則：根拠（記者の言及・具体データ）がない馬は、乖離が出ても価値があるとは扱わない。**

### レーティングが高くても配当に出ないケース（8/16発見）
ジャックルマロワ賞で8番ライフ（Rt117、出走馬中上位）を仮に☆相当(+2)で加点しても、EVはほぼ動かなかった（+0.02〜0.03程度）。理由は単勝6.4倍・4番人気という時点で市場がすでにその実力を織り込んでいたため。**「実力が高い」ことと「配当面で歪んでいる」ことは別問題。定性的な高評価だけでモデルを歪めても、市場が既に織り込んでいれば意味がない。**

---

## 3. 非馬@競馬予想（メイン情報源）の完全解読

### ★の定義（本人明記・資金配分ルール）
| 記号 | 意味 |
|---|---|
| ★なし | 自信無し（WIN5対象レースで出現） |
| ★ | 自信or馬券妙味少し |
| ★★ | **準勝負レース（半額で購入）** |
| ★★★ | **勝負レース（満額で購入）** |

### 印の使い分け（本人明記）
- **☆は基本的に3連系用**。ただし本命馬からの馬単の相手には流す可能性あり
- 「その他気になる穴馬」は**本人は買い目に入れない**（一発ある穴馬コーナー）

### ⚠️「見解追記中」は完成版で印が変わることがある（8/22発見）
中京7Rで「見解追記中」段階の表示は「◎⑦エムズマインド／△⑬セミマル」だったが、見解追記完了後の完成版では「◎⑬セミマル／☆⑦エムズマインド」に**◎と印そのものが入れ替わっていた**。**「見解追記中」の印だけを見て買い目を確定させないこと。**本文コメント（ポイント・◎○▲の解説）が揃った完成版を必ず待つ。

### 買い目の型（記事に明記されたパターン）
| 型 | 例 |
|---|---|
| 馬単◎→○ ／ 3連複◎-○-☆ ／ 3連単◎⇄○→▲(2点) | 8/8 札幌10R |
| 馬連◎-○ ／ 馬単◎→▲ ／ 3連単◎⇄○▲→○▲(4点) | 8/8 中京7R |
| 馬連◎-○▲△(3点) ／ 3連複◎-○▲△-○▲△☆(6点) | 8/9 札幌11R |

### 実際の資金配分（X的中報告＋8/22実購入画像から判明）
- **1レース集中型**：4,000〜8,000円を1レースに投下。分散しない
- **点数は4〜11点**
- **金額傾斜が極端**：100円〜3,500円と最大35倍の差。均等買いは一切なし
- **軸1頭から全券種を重ねる**：同じ相手を馬単・馬連・3連複の3層で押さえる
- **式別間の傾斜（8/22実例）**：馬連(1,200〜2,000円) > 馬単(300〜700円) ≒ 3連複(300〜600円)。**本線は馬連に集約**、他は保険で薄く広く
- **相手馬別の傾斜（8/22実例）**：▲が○より優遇されるケースがある（◎-▲がおの◎-○より厚い金額）。印の強さ通り（○>▲）ではなく、**配当妙味込みで判断している可能性が高い**
- 例外的に馬単だけ☆が最厚になっていたケースもあり、相手優先順位が式別ごとに変わることがある

⚠️ **注意**：X上の報告は「勝った回」のみ。回収率には選択バイアスがある。**構造は真似する価値があるが、成績そのものは真に受けないこと。**8/22の分析では、1レースの大勝ち(+21,600円)だけでは★★★5レース分散投資(想定40,000円規模)の収支を必ずしもカバーせず、5戦2勝を切るとトータルマイナスになりやすい構造であることが数値的に裏付けられた。

### 馬番ズレの確認済み事例（記事 → 公式）
| 馬名 | 記事 | 公式 | レース |
|---|---|---|---|
| チルカーノ | ② | **④** | 8/1 札幌11R |
| カシオペア | ⑥ | **⑧** | 8/1 新潟12R |
| キングメーカー | ⑥ | **⑤** | 8/1 中京7R |
| ロードアマルフィ | ⑤ | **③** | 8/2 札幌6R |
| 中京9R→10R | レース番号自体の誤記 | | 8/9 |

→ **毎回必ず照合すること。**（8/15・8/16は照合の結果、ズレなしだった。ズレは「必ず起きる」ものではなく「起こりうる」ものとして毎回チェックする）

---

## 4. Logic@競馬（副情報源）

- noteで無料予想を公開。**重賞は「最終結論」として◎○▲△★の5段階**
- 平場は馬名の簡易リストのみで印なし
- 「厳選レース」の詳細部分は検索で取得不可（スクショ/PDFが必要）
- 8/2の実績：クイーンS◎⑫ボンドガール（人気薄狙いを明言）、アイビスSD◎⑰ビッグシーザー（大外枠を評価）

---

## 5. 情報取得の手段と制約

### 使えるもの
- **netkeibaチェーン取得**：`https://race.sp.netkeiba.com/race/shutuba.html?race_id=XXXX` を起点に、隣接レース（±1R）へリンクで辿れる。**馬番・斤量・騎手・馬体重は取れる**
- **race_id構造**：`年(4) + 競馬場コード(2) + 回次(2) + 日目(2) + R番号(2)`。札幌=01、新潟=04、中京=07
- **Claude Codeで作成されたPDF**（出馬表オッズ買い目まとめ）が最も有用。印・オッズ・買い目候補が一枚に揃う
- **JRA-VAN Ver.World**（海外レース）：出馬表・オッズ・3連複オッズ（軸馬選択式）・馬連オッズ全表を個別スクリーンショットで取得可能（8/16ジャックルマロワ賞で実施）

### 使えないもの
- **netkeibaのオッズはJavaScript読み込みのため静的取得不可**（「---.-」と表示される）
- **オッズページのURLは直接fetchできない**（過去の検索結果に無いURLは拒否される）
- **keiba.go.jp / iwatekeiba.or.jp はrobots規制でブロック**
- **SPAT4は南関東4場（大井・川崎・船橋・浦和）専用**。笠松・園田・門別は対象外
- **X（旧Twitter）の個別投稿は検索エンジンに載らないため直接取得不可**。地方の平場戦は特に情報が薄い
- **netkeibaのレース結果ページは、レース直後〜一定期間プレミアム会員限定表示になり、着順詳細が取得できないことがある**（8/22中京7Rで発生。出馬表・オッズは取れるが結果テーブルは非表示）
- ライブのレース結果は検索反映が遅く、**たかひろさんのスクショが最速**

---

## 6. 実績記録

| 日付 | レース | 結果 |
|---|---|---|
| 6/28 | 函館記念 | 不的中 |
| 7/5 | 北九州記念 | 全見送り（±0）。買い候補6点は全ハズレで、規律が5,000円を守った |
| 7/6 | アルタイル賞（川崎） | 結果未照合 |
| 7/7 | 岩鷲賞（盛岡） | ワイド⑤-⑧的中 +270円 |
| 7/8 | スパーキングレディーC | 不的中 |
| 7/9 | スパーキングサマーチャレンジ | 全滅（1番人気⑬が11着の大波乱） |
| 7/12 | 七夕賞 | 全滅。⑪本命の読みは当たっていたが相手選定で共倒れ |
| 8/1 | 札幌12R（非馬の型） | +28,960円（投資8,000→払戻36,960、馬単⑤→②的中） |
| 8/2 | 非馬24点＋重賞10点 | 結果未照合 |
| 8/9 | 非馬33点＋Logic重賞 | 結果未照合 |
| 8/11 | クラスターカップ（岩鷲賞と同一舞台） | 結果未照合 |
| **8/15** | 非馬★★★5鞍のEV計算 | 頑健買い3点（札幌10R馬連⑫-⑤EV1.41／札幌11R馬連④-①EV1.20・3連複④①⑪EV1.15）。△5点、見送り9点。**中京11Rは4点全滅** |
| **8/16** | 非馬★★★5鞍のEV計算 | **頑健買いゼロ**（最も厳しい結果）。△6点のみ、残り全部見送り |
| **8/16** | ジャックルマロワ賞（海外G1） | 3連複⑩①③のみ頑健買い（EV1.46・頑健[1.28-1.78]）。他5点は見送り |
| **8/22** | 非馬★★★5鞍 | 札幌10R的中+21,600円（馬連⑤-⑨）。**札幌11R不的中確定**（◎⑧ピンクジン6着・▲③フクノブルーレイク競走中止）。中京7R・札幌7R・新潟10Rは結果未照合 ※PENDING |

### 8/1の勝因分析（再現性のある構造）
1. ◎から**印馬全員に馬単を流した**（◎→○／◎→☆／◎→☆／◎→▲の4方向）
2. 金額に強弱をつけ、当たった⑤→②に1,600円と厚めに置けていた
3. 同じ組み合わせを馬単・馬連・3連複で多層的に押さえていた

※このとき提示していた買い目は馬単◎→▲の1点だけで、当たり目の◎→☆が入っていなかった。**非馬が明記していた「本命馬からの馬単の相手には流す可能性あり」を読み落としていたのが原因。**

---

## 7. 海外レースへの応用（8/16新設）

- 非馬の加点方式（◎+8/○+5/▲+3、その他+2）は海外G1にも転用可能
- 印が6段階（◎○▲△△穴のような形式）の場合は、下位の印をまとめて☆相当(+2)扱いにして対応
- **単勝オッズは必ずJRA発売分を使う**。PMU（現地オッズ）は購入対象外なので参考値にとどめる
- **JRA/PMU比**（1.0超＝現地より日本で売れ残り＝妙味の可能性、1.0未満＝日本の方が過剰人気）は有効な補助指標
- ただし「人気の理由が薄い」ことと「実際に配当が歪んでいる」ことは別問題。定性判断だけで軽視/重視を決めず、組み合わせオッズが判明次第、必ずエンジンで検証する（③シックスペンス軽視→実は頑健買いだった事例、⑥ノーランチ重視→実は見送り相当だった事例が同一レースで両方発生）

---

## 8. 「完全な引き継ぎ」の運用制約と対策（8/16新設・重要）

### 技術的な限界
Claudeの新しい会話は、過去チャットの生のやり取りを自動で持ち越す仕組みがない。検索・読み込みツールで断片を参照し、その場で組み立て直すことはできるが、これは「引き継ぎ」ではなく「情報の再構築」であり、100%の再現は保証されない（実際に1章のインデックスバグとして表面化した）。

### 起きた実例
「競馬シミュレーション2」チャット（6月末〜8/9、300ターン超）が**チャット上限に到達し、二度と戻れなくなった**。

### 現実的な対策（確立した運用）
1. **この3点セット（keiba_engine_v2.py／test_engine.py／このhandoff.md）を、セッションの節目ごとに更新してダウンロード可能な形で出力する**
2. **次に新しいチャットを開いたら、この3点を必ず最初にアップロードして「これ踏まえて進めて」と伝える**
3. これにより、検索での再構築（不確実・時間がかかる）を回避し、正確性と速度の両方を担保できる
4. 完全に「あのチャットの全ターン」を再現することはできないが、**次の判断に必要な材料はここに集約する**という考え方で運用する

---

## 9. 継続事項（PENDING）

1. **8/9のレース結果照合**（非馬33点＋Logic分）※未着手のまま持ち越し
2. **8/11 クラスターカップ（盛岡）の結果照合** ※未着手のまま持ち越し
3. **8/22 中京7R・札幌7R・新潟10Rの詳細着順・収支確認**（netkeibaが結果テーブルを非表示にしており未確認）
4. **8/22 全体（★★★5鞍＋★4鞍＋無印1鞍）のトータル収支確定**
5. **8/23（日）のレースへの対応**（週末まとめPDFに一部情報あり、非馬予想はまだ未取得）

---

## 10. 運用上の原則（確立済み）

- **EV ≥ 1.0のみ購入**。人気馬でも軸から外すことを厭わない
- **見送りも戦略**。「買えないレースを買わなかった」のは負けではない
- **圧倒的1番人気は妙味ゼロの効率馬**パターンが頻出（岩鷲賞③・スパーキングレディー⑧・サマーチャレンジ⑬で3連続確認）
- **予想家が推す→オッズ短縮→妙味消滅**。note公開後に買われて妙味が減る現象を8/8中京7Rで確認（想定7.1倍→実際4.3倍）
- **少頭数（9頭前後）は市場が効率的で歪みが生まれにくい**。16頭以上の多頭数の方が妙味が出やすい
- **ワイド配当の自動推計は過大評価傾向**（実配当と3倍ずれた事例あり）。実オッズでの照合必須
- **1レースの大勝ちは複数レース分散投資の収支を必ずしも救わない**（8/22の分析）。的中率が一定を割ると、傾斜配分の設計上トータルマイナスになりやすい構造を数値で確認
- **定性的な評価とEV計算の結論はしばしば食い違う**（8/16ジャックルマロワ賞）。最終判断は必ずエンジンの数字を優先する
- 出力は**コンパクトなモバイル対応表示**、レース番号と馬番を明記、モデル限界を正直に開示

---

## 11. トーン

タメ口・絵文字入りで対応。ただし成果物（この種の資料含む）は用途に合った文体で。

---

## 12. Claude Code への移管（2026年8月23日）

### 何が変わったか
エンジン・テスト・この資料を**すべてリポジトリ `narita0130-bot/keiba-yosou` に格納**した。
これにより8章の「3点セットを毎回アップロードする運用」は不要になった。
セッションが切れても `git clone` すれば全部そろう。チャット上限で失われることもない。

### 標準ワークフロー（1日分）
```bash
cd /workspace/keiba-yosou

# 0) エンジンの健全性を確認（必須。落ちたら以降を実行しない）
cd engine && rm -rf __pycache__ && python3 test_engine.py && cd ..

# 1) 非馬の印を data/YYYY-MM-DD.ev.json に書く（race_id と marks のみ）
#    馬番の照合はこの時点で必ず行う（scripts/fetch_odds.py の race_info を使う）

# 2) EV計算
python3 engine/run_ev.py --spec data/2026-08-23.ev.json

# 3) 出馬表・オッズ・買い目のPDF（必要なら）
cd scripts
python3 build_sheet.py --spec ../data/2026-08-23.sheet.json --out /tmp/s.html
/opt/pw-browsers/chromium-1194/chrome-linux/chrome --headless --no-sandbox \
    --disable-gpu --no-pdf-header-footer --print-to-pdf=out.pdf file:///tmp/s.html
```

### run_ev.py の仕様
- 入力は `race_id` と `marks`（馬番→印）だけ。オッズと馬名はnetkeibaから自動取得
- 基礎能力は**単勝オッズのlog正規化（50〜80）＋印加点**で自動生成する。
  2章の方針をコードに落としたもので、無印馬は加点ゼロ＝乖離ゼロになる
- 他5項目は全馬3固定（2章の方針どおり）
- **EVは必ず実配当で計算する**（`actual_odds`）。複勝・ワイドはオッズ下限を採る保守側
- 候補は ◎の複勝／◎-相手のワイド・馬連／◎-相手2頭の3連複を自動生成
- 判定は 🟢頑健下限≥1.0 ／ 🟡中心EV≥1.0 ／ ❌それ未満

### PDFの配布について
生成直後のPDFは3〜5MBになり、モバイルでダウンロードが始まらない事例があった（8/22）。
配布前に必ず圧縮する：
```python
import pypdf
r=pypdf.PdfReader(src); w=pypdf.PdfWriter()
for p in r.pages: w.add_page(p)
for p in w.pages: p.compress_content_streams()
w.compress_identical_objects()
w.write(open(dst,'wb'))   # 実測 3.4MB → 0.93MB
```

### 環境メモ
- `race.netkeiba.com` は到達可能。**`world.jra-van.jp` はegressポリシーで403**（海外レースのオッズは画面提供が必要）
- `www.jra.go.jp` は到達可能（Shift-JIS。海外レースの公式出馬表PDFはここから取れる）
- 日本語PDFはChromium + IPAGothicで生成する
- **コンテナの時計が前後にずれることがある**（8/22→8/23で実害が出た）。
  日付は毎回 `TZ=Asia/Tokyo date` で明示的に確認し、オッズはAPIの `official_datetime` を正とする

---

## 13. 標準成果物と買い目テンプレート（9/6再整理）

### 標準成果物
1. **買い目シート（Artifact）** — 発走時刻順・レース名は表の左列・見出しなし。
2. **autofill CSV** — Chrome拡張「JRA CSV 自動入力」用。
3. **bets CSV** — グループID・オッズ取得時刻まで入った検証用フル列。

### 買い目テンプレート（印しかない場合）

```
馬連  : ◎軸流し、相手 = ◯▲△         （☆は入れない）
馬単  : ◎1着固定流し、相手 = ◯▲△     （☆は入れない）
3連複 : ◎1軸流し、相手 = ◯▲△☆ 全員   （C(n,2) 全点。☆同士のペアも残す）
```

`scripts/build_orders.py` がこの規則を実装している。**手で組まない。**

**☆だけを馬連・馬単から外す根拠**：note 本文に「☆は基本的に3連系用でつけています」と
明記がある。△にはその断りがないので ▲ と同格に扱う。

**3連複を▲の有無で分岐させない。** 相手集合は印そのもので決まる。実運用でも
9/5 札幌10R（◎5 ◯9 ▲14 ☆6 ☆11 → `05-06-11` を含む6点）と 9/5 中山9R
（→ `03-08-14` を含む6点）で ☆同士のペアを出している。8/29 新潟9R だけが
◯必須形（3点）だったが、あれは ☆同士の扱いを詰める前の古い挙動。

**やってはいけないこと**：☆同士のペアを間引く（JRAの1軸流し入力が使えなくなり手入力になる）。

## 14. 朝のEVは締切前に必ず取り直す

**朝に🟢だった買い目は、締切までに🟡や❌へ落ちる。** 妙味は消えるだけでなく、
売れ残った側へ移動する。

- 9/5：朝🟢だった7点が最終オッズで全部1.0を割り、代わりに朝ノーマークの人気薄2点が🟢になった。
- 9/6 阪神6R：12:35→12:45 の**10分**で全8点のオッズが下落。🟢3点のうち2点が🟡へ落ちた
  （馬連9-10 13.8→12.9、3連複5-9-10 41.8→35.9、3連複5-6-10 249.7→185.3で−25.8%）。

**netkeiba のオッズはレースごとに更新時刻が違う。** 正午に取ると、15時台のレースは
正午前後の値しか返らない。`bets.csv` の `odds_time` 列に取得時刻が入るので必ず確認する。
締切直前の値になっているのは発走が近いレースだけ。

## 15. 収支管理は自前で持つ

JRA-VAN は**ネイティブアプリ**なので拡張機能からは操作できない。買い目登録の自動化は不可能。
代わりに `fetch_result.py`（着順・払戻）と `settle.py`（突き合わせ・集計）で自前集計する。

```bash
python3 scripts/settle.py --bets data/YYYY-MM-DD.bets.csv --out data/YYYY-MM-DD.settle.csv
```

EV判定（buy/apparent/pass）別の回収率が出るので、エンジンの判定が機能しているかを検証できる。

## 16. データ消失の教訓（9/8）

**作業コンテナは破棄される。** 9/6 に作った全データと、9/5〜9/6 に書いた
`settle.py` / `fetch_result.py` / `build_bets_csv.py` / `build_orders.py` は
未pushのまま消えた（リポジトリの最終pushは8/26だった）。

さらに悪いことに、**過去レースのオッズは二度と取れない**。netkeiba のオッズAPIは
締切後のレースに対して HTTP 400 を返し、組み合わせオッズの過去分はどこにも公開されていない。
結果（払戻）だけは結果ページから取れるので「全部買った場合」の収支は後から計算できるが、
**EV判定は当時のオッズがなければ再現できない**ので、判定別の検証は永久に不可能になる。

→ **その日の作業が終わったら必ず ev.json / orders.json / bets.csv / settle.csv を
コミットする。** バックテストの材料は、その日のうちに保存しないと二度と手に入らない。

## 17. EV判定の実績（累計・要継続）

全点100円換算。**まだ判定の優劣を語れる段階にない。**

| 買い方 | 点数 | 投資 | 払戻 | 収支 | 回収率 | 的中 |
|---|---|---|---|---|---|---|
| 🟢のみ | 29 | 2,900円 | 0円 | −2,900円 | 0.0% | 0 |
| 🟢+🟡 | 58 | 5,800円 | 1,210円 | −4,590円 | 20.9% | 1 |
| ❌のみ | 67 | 6,700円 | 730円 | −5,970円 | 10.9% | 1 |
| **全部** | **126** | **12,600円** | **1,940円** | **−10,660円** | **15.4%** | **2** |

### 日別

- **9/5**（51点）：全滅。投資5,100円・払戻0円。判定内訳 🟢2 / 🟡13 / ❌35 / 判定なし1。
- **9/6**（75点）：投資7,500円・払戻1,940円・回収率25.9%・的中2点。
  🟢27点→0.0% ／ 🟡16点→75.6% ／ ❌32点→22.8%。

### 的中した2点（どちらも🟢ではない）

| 日 | レース | 式別 | 買い目 | 払戻 | EV | 判定 |
|---|---|---|---|---|---|---|
| 9/6 | 中山10R | 馬単 | 6→8 | 1,210円 | 1.003 | 🟡見かけ |
| 9/6 | 中山10R | 馬連 | 6-8 | 730円 | 0.983 | ❌見送り |

**注意すべき構造**：🟢は人気薄に偏る。◎+8 の加点が市場評価を大きく上回ったときに🟢が出るため、
**🟢＝穴狙いの集中投票**になっている。9/6の🟢27点のうち16点は札幌9R・札幌11Rの2鞍に集中し、
どちらも◎が9〜12人気で、まとめて沈んだ。分散が大きい買い方なので、
判定するには最低でも数百点、できれば1〜2か月の蓄積が要る。

### 9/6 に◎がどうなったか（参考）

| レース | ◎ | 人気 | 着順 |
|---|---|---|---|
| 阪神6R | 10 ザラタン | — | 8着 |
| 中山8R | 16 メディチパワー | 6人気 | **1着**（相手が飛んで全滅） |
| 札幌9R | 8 コスモオピニオン | 9人気 | 12着 |
| 中山9R | 14 イノキ | — | 10着 |

◎軸の1点流しは軸が消えた瞬間にレースごと終わる。この構造的リスクが9/6は繰り返し出た。
