# 競馬EV検討プロジェクト ─ AI向け実行仕様書

**このファイルの目的**：どのAI（Claude / ChatGPT / Gemini など）が担当しても、**同じ入力から同じ出力**が出るようにする。
曖昧な指示を排し、定数・式・分岐・丸めまで確定させてある。

**最初にこれを読んだAIへ**：勝手に方式を「改善」しないこと。仕様変更は人間の承認を得て、このファイルを更新してから行う。
判断に迷ったら §9「禁止事項」を優先する。

関連ファイル：`docs/handoff.md`（経緯・実績・学び）／`engine/`（実装）／`scripts/`（データ取得）

---

## 1. 前提環境

| 項目 | 内容 |
|---|---|
| 言語 | Python 3.11 |
| 依存 | `numpy`（エンジン）、`pypdf`・`pypdfium2`（PDF読み書き）。無ければ `pip install -q numpy pypdf pypdfium2` |
| 到達可能 | `race.netkeiba.com`（オッズ・出馬表）、`db.netkeiba.com`（過去走）、`www.jra.go.jp`（Shift-JIS。海外レース公式PDF） |
| **到達不可** | `world.jra-van.jp`（egressポリシーで403）。海外レースのオッズは人間からの画面提供が必須 |
| PDF生成 | Chromium `--headless --print-to-pdf` ＋ IPAGothic |

---

## 2. 起動時の必須手順（省略禁止）

```bash
cd engine && rm -rf __pycache__ && python3 test_engine.py
```

**「✅ 全21項目合格」が出るまで、いかなる買い目判断もしてはならない。**
1項目でも不合格なら、その旨を人間に報告して停止する。自分で修正して先に進まない。

続いて必ず日付を明示確認する。

```bash
TZ=Asia/Tokyo date "+%Y-%m-%d(%a) %H:%M JST"
```

> **コンテナの時計は前後にずれる実績がある**（2026-08-22→08-23で日付を1日取り違えた事故が発生）。
> 時刻の正はオッズAPIが返す `official_datetime`。日付を跨ぐ判断は必ずこれと突き合わせる。

---

## 3. 情報源と用語

### 主情報源：非馬@競馬予想（note）

| ★ | 意味 | 資金配分 |
|---|---|---|
| ★なし | 自信無し（WIN5対象レースで出現） | 買わない |
| ★ | 自信or馬券妙味少し | 任意・少額 |
| ★★ | 準勝負レース | **半額（4,000円目安）** |
| ★★★ | 勝負レース | **満額（8,000円目安）** |

- 印は ◎ ◯ ▲ △ ☆。**☆は基本的に3連系用**（本命からの馬単の相手に流す場合あり）
- 「その他気になる穴馬」は**本人は買い目に入れない**。こちらも入れない
- ☆に馬番だけが複数並ぶ表記がある（例 `☆4.7.16`）。**馬名なしで複数頭を指す**。見解本文の馬名と必ず突き合わせて確定させる
- 一部の◎が「スキ❤で表示」の限定公開になる。**人間から馬名を受け取るまで、その馬を推測してはならない**

### race_id の構造

`年(4) + 競馬場(2) + 回次(2) + 日目(2) + R番号(2)`
競馬場コード：01札幌 02函館 03福島 04新潟 05東京 06中山 07中京 08京都 09阪神 10小倉

---

## 4. netkeiba の仕様（実測ベース。推測で書き換えない）

### 4.1 レース一覧

```
https://race.netkeiba.com/top/race_list_sub.html?kaisai_date=YYYYMMDD
```
`(?:shutuba|result)\.html\?race_id=(\d+)` で抽出し、**重複除去してソート**。
**発走済みのレースはリンクが `result.html` に変わる**ため、`shutuba` だけを拾うと開催日当日に取りこぼす。

### 4.2 出馬表

```
https://race.netkeiba.com/race/shutuba.html?race_id=RACE_ID
```
行は `<tr class="HorseList" id="tr_N">`。馬番は `Umaban\d*[^>]*>\s*(\d+)`、馬名は `class="HorseName"><a ... title="..."`。

**枠順確定前の挙動（重要）**：枠・馬番が空欄になり、行は**五十音順**に並ぶ。
このとき `tr_N` の N を代替識別子として使う。**オッズAPIのキーもこの N と一致する**（5レースで照合済み）。
確定後の馬番とは無関係なので、シート上に必ず明記すること。

### 4.3 オッズAPI

```
https://race.netkeiba.com/api/api_get_jra_odds.html?type=TYPE&race_id=RACE_ID&action=init
Referer: https://race.netkeiba.com/odds/index.html?race_id=RACE_ID
```

| type | 式別 | レスポンスキー | 値の形 |
|---|---|---|---|
| 1 | 単勝＋複勝 | `"1"`＝単勝 / `"2"`＝複勝 | 単勝 `[オッズ, _, 人気]` / 複勝 `[下限, 上限, 人気]` |
| 4 | 馬連 | `"4"` | `[オッズ, _, 人気]` |
| 5 | ワイド | `"5"` | `[下限, 上限, 人気]` |
| 6 | 馬単 | `"6"` | `[オッズ, _, 人気]`（着順あり） |
| 7 | 3連複 | `"7"` | `[オッズ, _, 人気]` |
| 8 | 3連単 | `"8"` | `[オッズ, _, 人気]`（着順あり） |

- **キーの作り方**：馬番を2桁ゼロ埋めして連結。着順なし（複勝・ワイド・馬連・3連複）は**昇順にソートしてから**連結。着順あり（馬単・3連単）は**指定順のまま**連結
- **複勝とワイドは下限（`v[0]`）を採る**。上限を使ってはならない（保守側に倒す）
- `status == "NG"` は未発売。例外で停止し、値を推測しない

---

## 5. 印 → パラメータ変換（完全仕様）

エンジンには9要素タプル `(馬番, 馬名, 単勝オッズ, 基礎能力, 距離, コース, 騎手, 近走, 状態)` を渡す。

### 5.1 基礎能力

```
ln  = [log(単勝オッズ) for 全出走馬]
lo, hi = min(ln), max(ln)
if hi - lo < 1e-9:  baseline = 65.0（全馬）
else:               baseline_i = 80.0 - (80.0 - 50.0) * (ln_i - lo) / (hi - lo)

基礎能力_i = min(200.0, baseline_i + 印加点_i)
```

**印加点**：◎+8／◯（○）+5／▲+3／△+2／☆+2／★+2／穴+2／**無印 0**

### 5.2 他5項目

**距離・コース・騎手・近走・状態は全馬 3（中立）で固定**する。

> **なぜ固定するか**：情報のない馬に中立値を与えると、市場が厳しく評価している馬ほど相対的にモデルが高く見え、
> **根拠のない妙味が発生する**（川崎11Rで実際に発生）。
> 原則：**根拠（記者の言及・具体データ）がない馬は、乖離が出ても価値があるとは扱わない。**

### 5.3 エンジン設定

`RaceEngine(horses, lam=0.25, l2=0.81, l3=0.65)` ─ 既定値から変更しない。

- Plackett-Luce厳密計算（乱数なし・毎回同一結果）
- Henery割引 λ2=0.81 / λ3=0.65（Lo & Bacon-Shone研究値）
- 市場75% ＋ モデル25%（γはKL最小化で自動較正）
- 払戻率：単複0.80／枠連・馬連・ワイド0.775／馬単・3連複0.75／3連単0.725

---

## 6. EV計算と判定

### 6.1 EVは必ず実配当で計算する

```python
ev, p_hit, pay = eng.ev(kind, nums, actual_odds=実配当)
lo, hi         = eng.ev_robust(kind, nums, actual_odds=実配当)
```

`actual_odds` を省略すると市場確率からの理論値になる。**推定値を最終判断に使ってはならない。**

### 6.2 頑健性

`ev_robust` は λ∈{lam-0.15, lam, lam+0.15} × 割引∈{(1.0,1.0), (0.81,0.65), (0.70,0.50)} の**9通り**を評価し、最小と最大を返す。

### 6.3 判定記号

| 記号 | 条件 |
|---|---|
| 🟢 買い | **頑健下限 ≥ 1.0** |
| 🟡 見かけ | 中心EV ≥ 1.0 かつ 頑健下限 < 1.0 |
| ❌ 見送り | 中心EV < 1.0 |

**買いライン = 1 / 的中確率**。実配当がこのライン以上なら買い、未達なら見送り。

### 6.4 候補の自動生成（順序も固定）

◎を `a`、それ以外の印馬を印の登場順に `partners` とする。

1. 複勝 `(a,)`
2. ワイド `(a, b)` for b in partners
3. 馬連 `(a, b)` for b in partners
4. 3連複 `(a, partners[i], partners[j])` for i < j

**出力は頑健下限の降順にソート**する。

### 6.5 単勝の扱い

`_p_combo` は単勝に未対応。単勝を評価する場合のみ外部計算する（**エンジン本体は改変しない**）。

```python
p_hit = eng.p[eng.num2i[馬番]]
ev = p_hit * 実配当
```

---

## 7. 非馬の買い方を模倣する場合

EVとは**別の意思決定**であることを常に明示する。両者はしばしば矛盾する。

### 7.1 買い目の型（記事で確認済みのパターン）

- 馬単◎→◯ ／ 3連複◎-◯-☆ ／ 3連単◎⇄◯→▲(2点)
- 馬連◎-◯ ／ 馬単◎→▲ ／ 3連単◎⇄◯▲→◯▲(4点)
- 馬連◎-◯▲△(3点) ／ 3連複◎-◯▲△-◯▲△☆(6点)
- 馬連BOX◎◯▲ ／ 3連複◎-◯▲-◯▲☆

**記事に想定馬券が明記されている場合は、その型をそのまま使う。**

### 7.2 金額傾斜

- 1レース4,000〜8,000円。点数は**4〜11点**
- **均等買いはしない。**100円〜3,500円と最大35倍の傾斜をつける
- 式別：**馬連が本線**（1,200〜2,000円）＞ 馬単（300〜700円）≒ 3連複（300〜600円）
- 相手別：印の強さ（◯>▲）どおりとは限らない。**配当妙味込みで判断**する
- 軸1頭から馬連・馬単・3連複の**3層で重ねる**

### 7.3 出力に必ず添えるもの

型で組んだ買い目には、**EV判定を参考指標として各点に併記**し、
「型」と「EV」が食い違う場合はその旨を明示する。人間が選べる状態にして渡す。

---

## 8. 必ず実施する検証

| # | 検証 | 方法 |
|---|---|---|
| 1 | **馬番照合** | 記事の全印について、公式出馬表の馬番→馬名と一致するか機械照合。**記事の馬番は実際にズレる**（確認済み6件、レース番号自体の誤記も1件） |
| 2 | **見解の完成待ち** | 「見解追記中」段階の印は**完成版で変わることがある**（◎と印が入れ替わった実例あり）。完成版を待ってから買い目を確定する |
| 3 | **限定公開の◎** | 人間から馬名を受け取るまで空欄。推測禁止 |
| 4 | **枠順確定** | `draw_confirmed` を確認。未確定なら「並び」列として出し、馬番と一致しない旨を明記 |
| 5 | **日付** | §2のとおり |

濁点・音引きの表記ゆれ（例：アラレタバジル／アラレタバシル）は馬番が一致していれば**ズレではない**。公式表記を採用する。

---

## 9. 禁止事項

1. **オッズ・配当・着順を推測で書かない。**取得できなければ「取得不可」と書く
2. **回帰テスト不合格のまま買い目判断をしない**
3. **情報のない馬に中立値以外を与えない**（§5.2）
4. **エンジン本体（`keiba_engine_v2.py`）を改変しない**。必要な処理は外部スクリプトで行う
5. **推定配当で最終判断をしない**（§6.1）
6. **成績を誇張しない。**X上の的中報告は「勝った回」のみで選択バイアスがある。構造は参考にしてよいが成績は真に受けない
7. 仕様を勝手に変更しない。変更時は人間の承認＋このファイルの更新を伴う

---

## 10. 出力フォーマット

- モバイルで読める**コンパクトな表**。レース番号と馬番を必ず明記
- オッズには**必ず `official_datetime` を併記**する（発走まで変動するため）
- モデルの限界を正直に開示する。エンジンが考慮**しない**もの：枠順の有利不利／馬場状態への個別適性／展開・脚質の相互作用／斤量の増減効果
- PDFを配布する場合、生成直後は3〜5MBになりモバイルでダウンロードが始まらない事例がある。**必ず圧縮してから渡す**

```python
import pypdf
r=pypdf.PdfReader(src); w=pypdf.PdfWriter()
for p in r.pages: w.add_page(p)
for p in w.pages: p.compress_content_streams()
w.compress_identical_objects()
w.write(open(dst,'wb'))   # 実測 3.4MB → 0.93MB
```

---

## 11. 再現性の担保

**同じ入力なら必ず同じ出力になる**が、「同じ入力」には**オッズのスナップショット**が含まれる。

- エンジンは乱数を使わない。同じオッズ・同じ印なら小数以下まで一致する
- オッズは刻々と動く。**結果を比較・検証する場合は `official_datetime` が同一のデータで行う**
- 買い目・印は `data/YYYY-MM-DD.bets.json` と `data/YYYY-MM-DD.ev.json` に保存し、実行結果を口頭で持ち回らない

### 標準ワークフロー

```bash
cd engine && rm -rf __pycache__ && python3 test_engine.py && cd ..   # 0) 必須
# 1) data/YYYY-MM-DD.ev.json に race_id と marks を書く（馬番照合はここで実施）
python3 engine/run_ev.py    --spec data/YYYY-MM-DD.ev.json           # 2) EV計算
python3 engine/show_bets.py --spec data/YYYY-MM-DD.bets.json         # 3) 買い目を最新オッズで再評価
```

`bets.json` の各買い目は `["式別", [馬番...]]`（金額なし）または `["式別", [馬番...], 金額]`。
**金額の有無は自動判定**され、無ければ金額列・払戻列を省く。

---

## 12. 実績の記録

レース後は `docs/handoff.md` の実績表に追記する。記録する項目：

日付／レース／買い目／投資／払戻／収支／EV判定が当たったか外れたか。

### 2026-08-23 の検証結果（配分方式の比較）

同一の43点・的中4点（新潟6R 3点・中京7R 1点）に対し、配分方式だけを変えた実測。

| 方式 | 投資 | 払戻 | 回収率 |
|---|---|---|---|
| 傾斜（非馬の型） | 44,000 | 273,480 | **622%** |
| 全点フラット | 44,000 | 246,707 | 561% |
| レース別均等 | 44,000 | 218,959 | 498% |
| EV基準（🟢🟡のみ2,000円均等） | 8,000 | 274,600 | **3,433%** |

**得られた知見**

1. 傾斜そのものは有効。ただし全点フラットに対する上積みは約11%にとどまる
2. 「均等」の切り方で差が2倍近く変わる。均等にするなら**レース別ではなく全点フラット**
3. 型の傾斜（券種で決める）とEVの傾斜（歪みで決める）は別物。**この日はEVの傾斜が正しかった**
4. **馬単を切ってはいけない。**同日23ペアの平均EVは馬連0.771に対し馬単◎→0.835で、控除率が2.5%不利にもかかわらず馬単が上回った。◎への加点は「◎が勝つ」向きの歪みを作るため、着順を問わない馬連ではそれを換金できない。実際この日の最大配当は馬単4→14（137.3倍・事前EV🟢1.31）だった

※いずれも1日のサンプル。**一般化する前にサンプルを貯めること。**
